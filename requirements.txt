# NLP Semantic Mapping — Python dependencies
# Install with: pip install -r requirements.txt

# Core data
pandas
numpy
matplotlib

# Progress bars
tqdm

# Tokenization (used to filter out articles that are too long)
tiktoken

# Dimensionality reduction + clustering (HDBSCAN requires >=1.3)
scikit-learn>=1.3

# Interactive charts
plotly
nbformat>=4.2.0

# Jupyter notebook support
ipywidgets
ipykernel

# OpenAI API client
openai

# Load environment variables from .env
python-dotenv
