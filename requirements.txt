# NLP Semantic Mapping — Python dependencies
# Install with: pip install -r requirements.txt

# Core data
pandas
numpy
matplotlib

# Progress bars
tqdm

# Local embeddings (no API key needed, ~80 MB download on first run)
sentence-transformers

# Tokenization (used to filter out articles that are too long)
tiktoken

# Dimensionality reduction + clustering
scikit-learn

# Interactive charts
plotly

# Jupyter notebook support
ipywidgets
